node_modules/
.next/
# Next dev and isolated production-build outputs. These are generated artifacts
# and can contain hundreds of megabytes of webpack caches.
.next-dev/
.next-build/
.vercel/
dist/
.env
.env.local
*.log
exports/

# Downloaded Census bulk files and the 267k-row provider location dump.
# Large, immutable, and re-fetchable -- but never re-fetched on every run.
.cache/

# Playwright MCP scratch output
.playwright-mcp/
*.png

# Call recordings. PII, and re-fetchable from Retell only inside its short link
# window -- so they are backed up, never committed.
.recordings/
.recordings-smoke/

.vercel
.env*.local
